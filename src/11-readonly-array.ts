const numbers: ReadonlyArray<number> = [1, 2, 3, 4, 5];
// Solo serán permitidos los métodos inmutables, es decir, los que no modifiquen el array
// numbers.push(6);
// numbers.pop();
// numbers.unshift(0);
numbers.filter(() => {});
numbers.reduce(() => 0);
numbers.map(() => {});
